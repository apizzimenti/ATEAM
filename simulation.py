
import numpy as np
from Structures import Lattice, Simplex

# u = Simplex(np.array([1, 1, 0]))
# v = Simplex(np.array([1, 1, 0]))
# w = Simplex(np.array([2, 1, 0]))

# U = Simplex(np.array([u, v]))
# V = Simplex(np.array([v, w]))
# W = Simplex(np.array([u, w]))

# print(U == V, U == W, V == W)

L = Lattice([2, 2, 2], boundaryDimension=1)
